# Initialize RabbitMQ for examples

> The init_rabbitmq.py will create three users with tag 'cronio' and give permission to rd, wr, config everything.


	sudo pip install -r requirements
	python init_rabbitmq.py
